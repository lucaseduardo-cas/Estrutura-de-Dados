/*
3. Necessita-se de uma operação para inserir um elemento antes do primeiro. 
Implemente uma função chamada furaFila, que recebe como parametro o vetor,
que representa a fila propriamente dita; o valor a ser inserido; a posição 
do primeiro elemento dentro do vetor e a quantidade de elementos na fila 
(nItens) e realiza a operação desejada.

- A função de furarafila só poderá ser realizada se a fila não
estiver cheia.
*/

#include <stdio.h>
#include <stdlib.h>

int main(void) {

    return 0;
}